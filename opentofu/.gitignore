# What tofu writes next to a module. The state files stay out of git: the
# workflow keeps them in the Actions cache, as a stand-in for a real backend.
.terraform/
*.tfstate
*.tfstate.*
terraform.tfstate.d/
# What a deploy writes on the runner.
out/
